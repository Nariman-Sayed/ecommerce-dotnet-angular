# Ecom.UnitTests

This project is scaffolded ahead of need.

Unit tests will be added once the Application layer introduces real business logic
(validation rules, domain events, etc.). Until then, business logic is minimal
(anemic CRUD), and Integration tests (Ecom.IntegrationTests) cover the current
persistence behavior.

Mocking library: Moq (already referenced).
